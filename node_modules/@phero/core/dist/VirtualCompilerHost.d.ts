import ts from "typescript";
export interface TSFiles {
    [fileName: string]: string;
}
export declare class VirtualCompilerHost {
    private readonly compilerOpts;
    private readonly host;
    private readonly files;
    constructor(opts?: ts.CompilerOptions);
    addFile(fileName: string, source: string): void;
    getFile(fileName: string): string | undefined;
    createProgram(rootFileNames: string | string[]): ts.Program;
}
