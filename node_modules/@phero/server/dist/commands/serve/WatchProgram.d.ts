import ts from "typescript";
declare type BuildStartCallback = () => void;
declare type BuildSuccessCallback = (pheroSourceFile: ts.SourceFile, typeChecker: ts.TypeChecker) => void;
declare type BuildFailedCallback = (errorMessage: string) => void;
export default class WatchProgram {
    private readonly buildStartCallback;
    private readonly buildSuccessCallback;
    private readonly buildErrorCallback;
    readonly projectDir: string;
    private readonly tsConfigFilePath;
    private watchProgram?;
    constructor(absoluteProjectDir: string, buildStartCallback: BuildStartCallback, buildSuccessCallback: BuildSuccessCallback, buildErrorCallback: BuildFailedCallback);
    start(): void;
    close(): void;
    private reportDiagnostic;
    private reportWatchStatus;
    private onBuildStart;
    private onBuildComplete;
    private onBuildSuccess;
    private onBuildFailed;
}
export {};
