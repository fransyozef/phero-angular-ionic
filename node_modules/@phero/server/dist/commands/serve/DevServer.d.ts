import { ServerCommandServe } from "@phero/dev";
export interface PrintedClientCode {
    domainSource: string;
    basePheroClientSource: string;
    pheroClientSource: string;
}
export default class DevServer {
    private readonly command;
    private readonly projectPath;
    private readonly eventEmitter;
    private routes;
    private requestIndex;
    constructor(command: ServerCommandServe, projectPath: string);
    start(): Promise<void>;
    private get manifestPath();
    private get pheroExecutionJS();
    private startWatch;
    private startHttpServer;
    private buildProjectStart;
    private buildProjectSuccess;
    private buildProjectFailed;
    private requestHandler;
    private generateRoutes;
    private clearRequireCache;
}
